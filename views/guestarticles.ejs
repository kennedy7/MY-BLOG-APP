<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>BLOG</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="/send_url.js"></script>
  </head>
  <body>
    <header>
        <div class="header-nav">
          <a href="/">BLOG</a>
      
          <p>Welcome, Guest</p>
  
          <ul>
            <li><a href="/list">Articles</a></li>
            <li><a href="/login">Log in</a></li>
          </ul>
        </div>
        <p>My Blog project</p>
      </header>
  
      <main>
        <div class="article">
          <% if (article.category === 'all') { %>
            <h1><%= article.title %></h1>
            <p><%= article.content %></p>
          <% } %>
          <% if (article.category === 'limited') { %>
            <i>MEMBERS ONLY</i>
            <h1><%= article.title %></h1>
            
            <!-- Create an if statement with locals.isLoggedin as the conditional expression -->
            <% if (isAuthenticated) { %>
              <p><%= article.content %></p>
            <% } else { %>
              <div class="article-login">
                <p>Let's log in now!</p>
                <p>Please log in to read<br>the rest of the article.</p>
                <img src="/images/login.svg">
                <a class="btn" href="/login">Log in</a>
              </div>
            <% } %>
          <% } %>
        </div>
      </main>
    <footer>
      <a class="btn sub" href="/list">Back to articles</a>
    </footer>
  </body>
</html>
